name: Keep InsurEye Alive
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Call Streamlit App
        run: curl -L -v "https://insureye-deploy-vn4bc3cqlgbdlkmyntswdz.streamlit.app/" || true
